$(document).ready(function() {

  const urlParams = new URLSearchParams(window.location.search);
  var registerState = urlParams.get('registerState');

  if (registerState == 'success') {
    
  }

});
